# Dashboard Aurora — implémentation (2026-04-09)

**Goal :** Dashboard broker minimaliste thème Aurora, CSS extrait, Chart.js aligné sur tokens, a11y de base, injection `API_KEY` côté serveur.

**Livrables repo :** `dashboard/dashboard.css`, `dashboard/index.html` (placeholder `__API_KEY_JSON__`), `app/main.py` (`serve_dashboard` + `StaticFiles(html=False)`), tests `test_dashboard_contract.py`, `CallRepository(auto_seed=...)`, README mis à jour.
